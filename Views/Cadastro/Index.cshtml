@model ProjetoTi.Models.Usuario
@{
    
    ViewData["Title"] = "Cadastre-se";
    Layout = null;
}

<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />

    <!-- 🔹 Título exibido na aba do navegador -->
    <title>@ViewData["Title"] - SuporteTI IA</title>

    <style>
        /* 🎨 Paleta de cores e variáveis principais */
        :root {
            --bg: #f6f8fb;
            --card: #ffffff;
            --header-bg: #2563eb;
            --text: #111827;
            --header-text: #ffffff;
            --muted: #6b7280;
            --accent: #2563eb;
        }

        /* 🔹 Reset básico e estrutura de layout */
        body, html {
            margin: 0;
            padding: 0;
            height: 100%;
            font-family: Inter, system-ui, -apple-system, "Segoe UI", Roboto, Arial;
            background: var(--bg);
            color: var(--text);
        }

        /* 🔹 Centraliza o card na tela */
        .signup-page {
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 24px;
            box-sizing: border-box;
        }

        /* 🔹 Card principal do formulário */
        .card {
            background: var(--card);
            border-radius: 12px;
            box-shadow: 0 6px 18px rgba(16,24,40,0.06);
            width: 100%;
            max-width: 420px;
            display: flex;
            flex-direction: column;
            gap: 18px;
            overflow: hidden;
        }

        /* 🔹 Cabeçalho azul com nome e logo */
        .card-header {
            background: var(--header-bg);
            color: var(--header-text);
            padding: 20px;
            text-align: center;
        }

        /* 🔹 Ícone do logotipo */
        .brand .logo {
            width: 44px;
            height: 44px;
            border-radius: 10px;
            background: #1e40af;
            display: inline-flex;
            align-items: center;
            justify-content: center;
            color: white;
            font-weight: 700;
            font-size: 24px;
            margin-bottom: 8px;
        }

        /* 🔹 Título e subtítulo */
        .brand h1 {
            font-size: 22px;
            margin: 0;
        }

        .brand p {
            font-size: 13px;
            margin: 4px 0 0 0;
            color: var(--header-text);
        }

        /* 🔹 Corpo do card */
        .card-body {
            padding: 18px;
        }

        /* 🔹 Estrutura geral do formulário */
        form {
            display: flex;
            flex-direction: column;
            gap: 12px;
            width: 100%;
            box-sizing: border-box;
        }

        label {
            font-size: 14px;
            margin-bottom: 4px;
        }

        /* 🔹 Campos de entrada e seleção */
        input, select {
            padding: 10px;
            border-radius: 8px;
            border: 1px solid #d1d5db;
            font-size: 14px;
            width: 100%;
            box-sizing: border-box;
        }

        /* 🔒 Estrutura dos campos de senha */
        .password-wrapper {
            display: flex;
            width: 100%;
            gap: 8px;
        }

            .password-wrapper input {
                flex: 1;
            }

        /* 🔹 Botão de mostrar/ocultar senha */
        .toggle-btn {
            background: transparent;
            border: none;
            cursor: pointer;
            font-size: 18px;
            padding: 0 6px;
            color: var(--text);
        }

        /* 🔹 Botão principal de cadastro */
        .btn.primary {
            background: var(--accent);
            color: white;
            border: none;
            border-radius: 12px;
            padding: 10px 16px;
            cursor: pointer;
            font-weight: 600;
            font-size: 14px;
            margin-top: 8px;
        }

        /* 🔹 Rodapé e links de navegação */
        .signup, .card-footer {
            text-align: center;
            font-size: 13px;
            color: var(--muted);
        }

            .signup a, .link {
                color: var(--accent);
                text-decoration: none;
            }

                .signup a:hover, .link:hover {
                    text-decoration: underline;
                }

        /* 🔹 Mensagens de erro e sucesso */
        .form-message {
            margin-top: 8px;
            font-size: 13px;
            color: #dc2626; /* vermelho para erros */
        }

            .form-message.success {
                color: #16a34a; /* verde para sucesso */
            }

        /* 📱 Responsividade */
        @@media (max-width: 520px) {
            .card {
                max-width: 92%;
            }

            .brand h1 {
                font-size: 20px;
            }
        }
    </style>
</head>

<body>
    <main class="signup-page">
        <div class="card">
            <!-- 🔹 Cabeçalho com logotipo e nome -->
            <header class="card-header">
                <div class="brand">
                    <div class="logo">🔧</div>
                    <div>
                        <h1>SuporteTI IA</h1>
                        <p class="tagline">Cadastre-se para acessar o sistema</p>
                    </div>
                </div>
            </header>

            <!-- 🔹 Corpo do card -->
            <section class="card-body">

                <!-- ✅ Exibe mensagem de sucesso vinda do Controller -->
                @if (TempData["Success"] != null)
                {
                    <div class="form-message success">@TempData["Success"]</div>
                }

                <!-- ⚠️ Exibe erros de validação do ModelState -->
                @if (ViewData.ModelState.ErrorCount > 0)
                {
                    <div class="form-message">@Html.ValidationSummary()</div>
                }

                <!-- 🔹 Formulário de cadastro -->
                <form asp-action="Index" method="post" novalidate>
                    @Html.AntiForgeryToken() <!-- proteção contra ataques CSRF -->
                    <!-- Campo nome -->
                    <label for="Nome">Nome</label>
                    <input asp-for="Nome" id="Nome" name="Nome" type="text" placeholder="Seu nome completo" required />

                    <!-- Campo e-mail -->
                    <label for="Email">E-mail</label>
                    <input asp-for="Email" id="Email" name="Email" type="email" placeholder="seu@exemplo.com" required />

                    <!-- Campo senha -->
                    <label for="Senha">Senha</label>
                    <div class="password-wrapper">
                        <input asp-for="Senha" id="Senha" name="Senha" type="password" placeholder="••••••••" required minlength="6" />
                        <button type="button" class="toggle-btn" id="togglePassword" aria-label="Mostrar senha">👁️</button>
                    </div>

                    <!-- Campo confirmação de senha -->
                    <label for="confirmPassword">Confirmar Senha</label>
                    <div class="password-wrapper">
                        <input id="confirmPassword" name="confirmPassword" type="password" placeholder="••••••••" required minlength="6" />
                        <button type="button" class="toggle-btn" id="toggleConfirmPassword" aria-label="Mostrar senha">👁️</button>
                    </div>

                    <!-- Tipo de usuário -->
                    <label for="Papel">Tipo de usuário</label>
                    <select asp-for="Papel" id="Papel" name="Papel" required>
                        <option value="colaborador">Colaborador / Cliente</option>
                        <option value="tecnico">Técnico</option>
                    </select>

                    <!-- Botão principal -->
                    <button class="btn primary" type="submit">Cadastrar</button>

                    <!-- Link para login -->
                    <div class="signup">
                        <span>Já tem uma conta?</span>
                        <a class="link" asp-controller="Login" asp-action="Index">Entrar</a>
                    </div>

                    <!-- Área de mensagens dinâmicas -->
                    <div id="formMessage" role="status" class="form-message" aria-live="polite"></div>
                </form>
            </section>

            <!-- 🔹 Rodapé informativo -->
            <footer class="card-footer">
                <small>Universidade • Projeto de Sistemas • LGPD aplicada</small>
            </footer>
        </div>
    </main>

    <!-- 🔹 Script para mostrar/ocultar senhas e validar o formulário -->
    <script>
        (function(){
          // Alterna a exibição da senha principal
          const togglePassword = document.getElementById('togglePassword');
          const passwordInput = document.getElementById('Senha');
          if(togglePassword && passwordInput){
            togglePassword.addEventListener('click', () => {
              passwordInput.type = passwordInput.type === 'password' ? 'text' : 'password';
            });
          }

          // Alterna a exibição da confirmação de senha
          const toggleConfirm = document.getElementById('toggleConfirmPassword');
          const confirmInput = document.getElementById('confirmPassword');
          if(toggleConfirm && confirmInput){
            toggleConfirm.addEventListener('click', () => {
              confirmInput.type = confirmInput.type === 'password' ? 'text' : 'password';
            });
          }

          // Validação: senhas devem coincidir
          const form = document.querySelector('form');
          const msgEl = document.getElementById('formMessage');

          if(form){
            form.addEventListener('submit', (e) => {
              const senha = document.getElementById('Senha')?.value || '';
              const confirm = document.getElementById('confirmPassword')?.value || '';

              // Se as senhas forem diferentes, bloqueia o envio
              if(senha !== confirm){
                e.preventDefault();
                if(msgEl) msgEl.textContent = "As senhas não coincidem!";
              } else {
                if(msgEl) msgEl.textContent = "";
              }
            });
          }
        })();
    </script>
</body>
</html>
